#include <iostream>
#include <vector>
using namespace std;


int main() {
	int x, y;
	cin >> x >> y;
	vector<vector<int> (y)> m (x);
	for (int i = 0;i < x;++i) {
		for (int j = 0; i < y; ++i) {
			cin >> m[i];
		}
	}
}